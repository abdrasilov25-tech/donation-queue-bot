async function handlePay(ctx) {
  await ctx.reply(
    `💳 <b>Реквизиты для доната</b>\n` +
    `━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n` +
    `📱 <b>Kaspi</b>\n` +
    `Номер: <code>87712472645</code>\n` +
    `Получатель: <b>Назипа А.</b>\n\n` +
    `━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n` +
    `⚠️ <b>Важно:</b>\n` +
    `• В комментарии к переводу укажите своё имя\n` +
    `• Минимальная сумма: <b>100 ₸</b>\n` +
    `• После перевода подайте заявку через /start\n` +
    `• Приложите скриншот — без него заявка не принимается\n\n` +
    `━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n` +
    `/start — подать заявку\n` +
    `/balance — текущий счёт системы`,
    { parse_mode: 'HTML' }
  );
}

module.exports = { handlePay };
