async function handlePay(ctx) {
  await ctx.reply(
    `💳 *Реквизиты для доната*\n` +
    `${'━'.repeat(28)}\n\n` +
    `📱 *Kaspi*\n` +
    `Номер: \`87712472645\`\n` +
    `Получатель: *Назипа А.*\n\n` +
    `${'━'.repeat(28)}\n\n` +
    `⚠️ *Важно:*\n` +
    `• В комментарии укажите своё имя\n` +
    `• После перевода подайте заявку через /start\n` +
    `• Приложите скриншот перевода — без него заявка не принимается\n\n` +
    `${'━'.repeat(28)}\n` +
    `/start — подать заявку\n` +
    `/balance — текущий счёт системы`,
    { parse_mode: 'Markdown' }
  );
}

module.exports = { handlePay };
